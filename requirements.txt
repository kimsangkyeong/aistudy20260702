# ====================================================================
# [Layer 1] 환경 관리 및 네트워크 코어 
# ====================================================================
python-dotenv==1.0.1          # .env 파일을 읽어 회사/집 환경 스위칭 제어
requests==2.32.3              # Streamlit 프론트와 FastAPI 백엔드 간 REST API 통신 제어

# ====================================================================
# [Layer 2] FastAPI 백엔드 계층
# ====================================================================
fastapi==0.111.0              # 에이전트 오케스트레이션을 제공할 메인 BE 프레임워크
uvicorn==0.30.1               # FastAPI 고성능 비동기 구동을 위한 ASGI 웹 서버 엔드포인트

# ====================================================================
# [Layer 3] 프론트엔드 UI 계층 
# ====================================================================
streamlit==1.35.0             # 플레이북 및 멀티턴 대화 이력 렌더링 다중 페이지 통합 웹 UI

# ====================================================================
# [Layer 4] LangChain 코어 프레임워크
# ====================================================================
langchain==0.2.5              # 대형 생태계 추상화 및 데이터 파이프라인 코어
langchain-core==0.2.9         # 프롬프트 템플릿, 메시지 객체, 스키마 정의 규격
langchain-community==0.2.5    # FlashRank, 웹 검색 등 외부 커뮤니티 도구 연동 허브

# ====================================================================
# [Layer 5] LangGraph 워크플로우 맵 
# ====================================================================
langgraph==0.1.4              # Router, Expert, Code_Validator 간 4-Node 교차 검증 그래프 빌드

# ====================================================================
# [Layer 6] LLM 공급자 연동 (Multi-Vendor 인터페이스)
# ====================================================================
langchain-openai==0.1.9       # 사내 인프라 Azure OpenAI(GPT-4o, Embed-3) 전용 드라이버
langchain-google-genai==1.0.6 # 집 전용 무료 인프라 Google Gemini Pro 및 임베딩 연동 모듈

# ====================================================================
# [Layer 7] 고정밀 RAG 인프라 및 전처리 팩토리
# ====================================================================
pydantic==2.7.4               # 최종 출력 구조화(OCPResponseSchema) 및 타입 검증 파서
pypdf==4.2.0                  # Red Hat 공식 4.20 설치/운영 PDF 가이드북 파일 텍스트 덤프용
chromadb==0.5.3               # 7권의 문서(2,343개 청크)를 영구 적재하는 파일 기반 로컬 Vector DB
langchain-chroma==0.1.1       # [⚠️ 보완 반영] Chroma와 LangChain 표준 데이터 연동을 매핑하는 필수 확장 라이브러리
flashrank==0.2.8              # 유사도 검색 상위 문서를 고정밀 재정렬하는 FlashRank Reranker 코어 엔진